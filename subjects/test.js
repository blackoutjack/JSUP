var glob = "25";

function tester(p1, p2) {
    var x = changeMe(getElementById())("more");
    var w = diggit(1,2);
    var y = "yes";
    var z = 9 /5/ 2;
    var zz = 4/ 2;
    test1(glob);
    var test1 = "false";
    _trackEvent(a, b , c);
    var maybe = /yes|no/.match("yes");
    webkit = /webkit/.test(u) ? parseFloat(u.replace(/^.*\dwebkit*(\d+(+)?).*$/, "$1")) : false; // returns either the webkit version or false if not webkit
    /* here's a comment */
    var hgt = x.style.height;

    // here's antoher comment
    return p1 + p2 + hgt + glob;
}

changeMe(12);


